

```
满足“距离停止线≤max_valid_stop_distance 且灯已变绿”时，Stage 调用 FinishStage()，把当前灯 ID 写入 planning_context.traffic_light.done_traffic_light_overlap_id，允许后续规则跳过该灯。

这个距离停止线的距离的变量在哪个文件里？如何修改代码，在仿真运行时将这个变量打印在某个特别指定的文件中？使用AINFO还是直接c++更通用的文字写入比较好？
```

```bash
buildtool profile config init --package planning --profile=default
```

将
`profiles/default/modules/planning/planning_component/conf/planning.conf`的`--enable_print_curve`改为`false`可以使得`application-core/data/log`下的`planning.log.INFO.20260122-190600.21006`可以显示，但是很难找到有用信息