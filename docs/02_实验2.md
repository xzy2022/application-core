
## 赛题提交
提交网址`https://apollo.baidu.com/community/competition`
```bash
# 只修改配置文件
tar -zcvf 自己定义名字.tar.gz  profiles/default

# 修改配置文件和源码
tar -zcvf xzy.tar.gz modules/planning/ profiles/default
```

## 获取配置文件

```bash
buildtool profile config init --package planning-traffic-rules-traffic-light --profile=default
aem profile use default
```
获取的文件情况为
```bash
[xzy@in-dev-docker:/apollo_workspace]$ tree profiles/default/modules/planning/traffic_rules/
profiles/default/modules/planning/traffic_rules/
`-- traffic_light
    `-- conf
        `-- default_conf.pb.txt

2 directories, 1 file
```

## 实验场景设置
在浏览器中下载`apollo_virutal_map`，并通过`watch -n 1 ls -lh /apollo/data/map_data/`查看下载进度。
```
模式：PnC
模块：Planning Prediction
操作：Scenario_Sim
环境资源：
    场景：
        国防科技大学：
            xh-交通灯路口通行1
车辆：Mkz example
```

## 流程分析

```bash
# 实验显示
LANE_FOLLOW                      STAGE
TRAFFIC_LIGHT_PROTECTED          APPROACH
TRAFFIC_LIGHT_PROTECTED          INTERSECTION_CRUISE
LANE_FOLLOW                      STAGE   

# 分层决策
TrafficLightProtectedStage
TrafficLightProtectedStageApproach
TrafficLightProtectedStageIntersectionCruise
```


## 增大交换内存
```bash
# 1. (再次确认) 查看当前磁盘空间
df -h /

# 2. 分配一个 16GB 的文件 (在根目录下，这会占用你 Ubuntu 的 16GB 硬盘空间)
sudo fallocate -l 16G /swapfile_extra

# 3. 设置只有 root 读写权限 (为了安全)
sudo chmod 600 /swapfile_extra

# 4. 把这个文件格式化为 Swap 格式
sudo mkswap /swapfile_extra

# 5. 立刻启用它 (救命的一步！)
sudo swapon /swapfile_extra

# 6. 验证是否成功
free -h

# 备份一下配置文件 (好习惯)
sudo cp /etc/fstab /etc/fstab.bak

# 把 swapfile 配置追加到 /etc/fstab 文件末尾
echo '/swapfile_extra none swap sw 0 0' | sudo tee -a /etc/fstab

# 看最后一行是不是多了 /swapfile_extra ...
cat /etc/fstab
```